// Este arquivo existe apenas para satisfazer o cache do bundler
// A funcionalidade de impressao foi movida para o componente ordem-card.tsx

export function gerarPDF() {
  // Funcao vazia - impressao agora e feita diretamente no componente
  console.log('Funcao deprecada - use o botao Imprimir no card da OS')
}
